[package]
name = 'RS'
version = '0.1.0'
edition = '2021'

[lib]
crate-type = ['staticlib']
name = 'RS'

[dependencies]
extendr-api = { version = "*", features = ['non-api'] }
profiling = "1.0.16"

[profile.release]
debug = true
lto = "fat"
opt-level = 3
incremental = false
codegen-units = 1
